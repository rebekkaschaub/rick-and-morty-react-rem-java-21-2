function Character({character} ) {

    return <section>
        <h2 className={"super-header"}>
            {character.name}
        </h2>
        some description
    </section>
}

export default Character;
